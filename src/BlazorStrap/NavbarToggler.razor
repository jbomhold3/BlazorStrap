@inherits BootstrapComponentBase

<DynamicElement TagName="button" class="@classname" aria-label="Toggle navigation" @onclick="@onclick" @attributes="UnknownParameters">
    <span class="navbar-toggler-icon"></span>
</DynamicElement>

@code {
    private string classname =>
        new CssBuilder("navbar-toggler")
            .AddClass(Class)
        .Build();

    [Parameter] private EventCallback<UIMouseEventArgs> onclick { get; set; }
    [Parameter] private string Class { get; set; }
}